import { MovieList } from "./movie-list.model";
import { Movie } from "./movie.model";

export class MovieListEntry {
    MovieListId: number;
    MovieList: MovieList;
    MovieId: number;
    Movie: Movie;
}
